# runtime

`runtime/python` is a local bundled Python runtime for source-run delivery.

- It is prepared on the developer machine.
- It is shipped together with the project folder to user machines.
- It is intentionally ignored by Git and should not be committed.

Prepare it with:

```bat
准备运行时.bat
```
